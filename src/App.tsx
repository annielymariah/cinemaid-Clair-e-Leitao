import { AppRouter } from "./routes/AppRoutes";	
import { useEffect } from "react";
//import "src/app/global.css"

export const App = () => {
  useEffect(() => {
    fetch("")
  }, []);

  return <AppRouter />;
};